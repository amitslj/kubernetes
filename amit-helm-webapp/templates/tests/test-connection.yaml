apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "amit-helm-webapp.fullname" . }}-test-connection"
  labels:
    {{- include "amit-helm-webapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "amit-helm-webapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

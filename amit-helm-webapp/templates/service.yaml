apiVersion: v1
kind: Service
metadata:
  name: {{ include "amit-helm-webapp.fullname" . }}
  labels:
    {{- include "amit-helm-webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "amit-helm-webapp.selectorLabels" . | nindent 4 }}
